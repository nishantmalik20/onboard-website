# Server
PORT=3001

# SMTP Configuration (leave blank for console-only logging in development)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password

# Email address that receives contact/quote notifications
NOTIFICATION_EMAIL=contact@onboardprints.ca
